# LAB 8: NAT (Network Address Translation) конфигурация

**Продължителност:** 75-90 минути  
**Цел:** Научаване на NAT конфигурация за симулиране на връзка с интернет и споделяне на публични IP адреси

---

## ЦЕЛИ НА УПРАЖНЕНИЕТО

След завършване на този лаб ще можете да:
- ✅ Разбирате какво е NAT и защо се използва
- ✅ Конфигурирате Static NAT (1:1 mapping)
- ✅ Конфигурирате Dynamic NAT (pool-based)
- ✅ Конфигурирате PAT (Port Address Translation) - най-често използван
- ✅ Симулирате реалистична връзка с интернет в Packet Tracer
- ✅ Troubleshoot NAT проблеми
- ✅ Подготвите се за интегрирания Lab 9

---

## ТЕОРЕТИЧНА ОСНОВА

### Какво е NAT?

**Network Address Translation (NAT)** е техника за транслиране на private IP адреси към public IP адреси при комуникация с интернет.

**Защо е нужен NAT?**
- 📊 Липса на достатъчно IPv4 адреси
- 🔒 Сигурност (скрива вътрешната топология)
- 💰 Икономия (не е нужен публичен IP за всяко устройство)
- 🔄 Лесна промяна на ISP (само външният IP се променя)

### Типове NAT:

#### 1. **Static NAT** (1:1 mapping)
```
Inside Local    ←→    Inside Global
192.168.1.10    ←→    203.0.113.10
192.168.1.11    ←→    203.0.113.11
```
- Перманентно 1:1 mapping
- Използва се за сървъри (Web, Mail, FTP)
- Нужен е отделен публичен IP за всяко устройство

#### 2. **Dynamic NAT** (Pool-based)
```
Inside Local         Inside Global Pool
192.168.1.10    →    203.0.113.10-20 (избира се свободен)
192.168.1.11    →    203.0.113.11 (ако има свободен)
```
- Временно mapping от pool
- First-come, first-served
- Ако pool-ът е пълен, новите връзки чакат

#### 3. **PAT** (Port Address Translation) - NAT Overload
```
Inside Local          Inside Global (IP:Port)
192.168.1.10:54321 → 203.0.113.1:54321
192.168.1.11:54322 → 203.0.113.1:54322
192.168.1.12:54323 → 203.0.113.1:54323
```
- Споделя един публичен IP за много устройства
- Използва port numbers за разграничаване
- **Най-често използван** в домашни и малки мрежи
- До 65,535 едновременни връзки на IP адрес

---

## ТОПОЛОГИЯ

### Целева топология:

```
    ┌─────────────────────────────┐
    │   INTERNET SIMULATION       │
    │   ISP-Router (2911)         │
    │   - Loopback0: 8.8.8.8/32   │
    │   - Loopback1: 1.1.1.1/32   │
    └──────────┬──────────────────┘
               │ Se0/0/0 (DCE) 
               │ 209.165.200.1/30
               │ clock rate 64000
               │
               │ Se0/0/0 (DTE)
               │ 209.165.200.2/30
        ┌──────▼──────┐
        │   R1-NAT    │ (NAT Router 2911)
        │             │
        └──────┬──────┘
               │ G0/0 (inside)
               │ 192.168.10.1/24
               │
        ┌──────▼──────┐
        │    SW1      │ (Switch 2960-24TT)
        └─┬────────┬──┘
          │        │
    ┌─────▼──┐  ┌──▼──────┐
    │  PC1   │  │ Server1 │
    │ .10.10 │  │ .10.50  │
    └────────┘  └─────────┘
     DHCP/PAT   Static NAT

Network Details:
- Internal Network: 192.168.10.0/24
- Public IP range: 203.0.113.0/24 (Test-Net-3, RFC 5737)
- ISP WAN link: 209.165.200.0/30
- Simulated Internet: 8.8.8.8, 1.1.1.1 (Loopbacks)
```

### Защо ISP Router вместо Cloud?

**Проблеми с Cloud устройството:**
- ❌ Не може да се конфигурира като router
- ❌ Няма routing capabilities
- ❌ Не може да симулира DNS/Internet services
- ❌ Ограничена функционалност

**Предимства на ISP Router:**
- ✅ Реалистична симулация на ISP
- ✅ Loopback интерфейси за симулация на интернет услуги (8.8.8.8, 1.1.1.1)
- ✅ Пълна routing конфигурация
- ✅ Подготовка за реални мрежови сценарии
- ✅ Compatibility с Lab 9

---

## ЧАСТ 0: Изграждане на ISP инфраструктура

### СТЪПКА 0: Създаване на базовата топология

В Packet Tracer:
- **2x Router 2911** (ISP-Router и R1-NAT)
- **1x Switch 2960-24TT** (SW1)
- **2x PC** (PC1, Server1)

**Кабелиране:**
```
ISP-Router Se0/0/0 (DCE) ↔ R1-NAT Se0/0/0 (DTE)
R1-NAT Gi0/0 ↔ SW1 Gi0/1
SW1 Fa0/1 ↔ PC1
SW1 Fa0/2 ↔ Server1
```

**ВАЖНО: DCE vs DTE**
- **ISP-Router Se0/0/0** = **DCE** (има `clock rate 64000`)
- **R1-NAT Se0/0/0** = **DTE** (няма clock rate)

> 💡 В Packet Tracer, DCE страната задава clock rate. За да проверите коя е DCE страната, погледнете кабела - има малък часовник символ.

---

### СТЪПКА 0.1: Конфигурация на ISP Router (симулира интернет)

```cisco
enable
configure terminal
hostname ISP-Router
no ip domain-lookup

! Loopback интерфейси (симулират интернет услуги)
interface loopback 0
 description Simulated Internet - Google DNS
 ip address 8.8.8.8 255.255.255.255
 no shutdown
 exit

interface loopback 1
 description Simulated Internet - Cloudflare DNS
 ip address 1.1.1.1 255.255.255.255
 no shutdown
 exit

! WAN интерфейс към клиента (DCE страна)
interface serial 0/0/0
 description WAN to Customer R1-NAT
 ip address 209.165.200.1 255.255.255.252
 clock rate 64000
 no shutdown
 exit

! Static routes към customer network (симулира BGP routing)
ip route 203.0.113.0 255.255.255.0 209.165.200.2
ip route 192.168.10.0 255.255.255.0 209.165.200.2

! Enable IP routing
ip routing

! Banner
banner motd #
*************************************************
*  ISP ROUTER - SIMULATED INTERNET ACCESS       *
*  Available services: 8.8.8.8, 1.1.1.1         *
*************************************************
#

end
copy running-config startup-config
```

**Обяснение на ISP Router конфигурацията:**
- `Loopback 0` и `Loopback 1` симулират публични интернет услуги (DNS сървъри)
- `clock rate 64000` е задължителен на DCE страната за синхронизация
- Static routes позволяват на ISP да достига до customer мрежата (за тестване)

---

## ЧАСТ 1: Static NAT (за сървъри)

### СТЪПКА 1: Базова конфигурация на R1-NAT Router

```cisco
enable
configure terminal
hostname R1-NAT
no ip domain-lookup

! Inside interface (към вътрешната мрежа)
interface gigabitEthernet 0/0
 description Inside Network - LAN
 ip address 192.168.10.1 255.255.255.0
 no shutdown
 exit

! Outside interface (към ISP/Internet) - DTE страна
interface serial 0/0/0
 description Outside to ISP - WAN
 ip address 209.165.200.2 255.255.255.252
 no shutdown
 exit

! Default route (всичко неизвестно отива към ISP)
ip route 0.0.0.0 0.0.0.0 209.165.200.1

! Banner
banner motd #
*************************************************
*  R1-NAT ROUTER - CUSTOMER EDGE ROUTER         *
*  NAT Configuration Lab                        *
*************************************************
#

end
copy running-config startup-config
```

**Обяснение:**
- `ip address 209.165.200.2` - DTE страната на WAN връзката
- **НЯМА** `clock rate` на DTE интерфейса (само ISP-Router има clock rate)
- `ip route 0.0.0.0 0.0.0.0` - default route изпраща всичко неизвестно към ISP

---

### СТЪПКА 2: Проверка на connectivity преди NAT

```cisco
! От R1-NAT, ping ISP Router
ping 209.165.200.1

! Ping loopback интерфейсите на ISP (симулиран интернет)
ping 8.8.8.8
ping 1.1.1.1
```

**Очаквани резултати:**
```
Type escape sequence to abort.
Sending 5, 100-byte ICMP Echos to 8.8.8.8, timeout is 2 seconds:
!!!!!
Success rate is 100 percent (5/5), round-trip min/avg/max = 1/1/2 ms
```

✅ Ако ping-овете работят, WAN връзката е правилна!

---

### СТЪПКА 3: Конфигуриране на Static NAT за Server

**Сценарий:** Server1 е Web server на 192.168.10.50 и трябва да е достъпен от интернет на публичен IP 203.0.113.50

```cisco
configure terminal

! Дефиниране на inside и outside интерфейси
interface gigabitEthernet 0/0
 ip nat inside
 exit

interface serial 0/0/0
 ip nat outside
 exit

! Static NAT mapping (inside local → inside global)
ip nat inside source static 192.168.10.50 203.0.113.50

end
copy running-config startup-config
```

**Обяснение:**
- `ip nat inside` - маркира интерфейса като вътрешен
- `ip nat outside` - маркира интерфейса като външен
- `ip nat inside source static` - създава перманентно 1:1 mapping

---

### СТЪПКА 4: Тестване на Static NAT

#### 4.1 Конфигурирай устройствата

**Server1:**
- IP: `192.168.10.50`
- Mask: `255.255.255.0`
- Gateway: `192.168.10.1`
- DNS: `8.8.8.8`

**PC1:**
- IP: `192.168.10.10`
- Mask: `255.255.255.0`
- Gateway: `192.168.10.1`
- DNS: `8.8.8.8`

#### 4.2 Провери NAT translations

```cisco
show ip nat translations
```

**Очакван изход:**
```
Pro Inside global      Inside local       Outside local      Outside global
--- 203.0.113.50       192.168.10.50      ---                ---
                       ^^^^^^^^^^^^       ^^^^^^^^^^
                       Private IP         Public IP
```

#### 4.3 Тестове

**Тест 1: Вътрешен ping към сървъра (преди NAT)**
От PC1:
```
C:\> ping 192.168.10.50
```
✅ Трябва да работи (локална мрежа)

**Тест 2: Външен ping към публичния IP (след NAT)**
От ISP-Router:
```cisco
ping 203.0.113.50 source loopback 0
```
✅ Трябва да работи (NAT translation)

**Тест 3: Ping от Server към симулиран интернет**
От Server1:
```
C:\> ping 8.8.8.8
```
⚠️ Още НЕ работи - нужен е PAT за outbound трафик!

#### 4.4 Статистики

```cisco
show ip nat statistics

! Пример изход:
! Total active translations: 1 (1 static, 0 dynamic; 0 extended)
! Outside interfaces: Serial0/0/0
! Inside interfaces: GigabitEthernet0/0
! Hits: 10  Misses: 0
```

---

## ЧАСТ 2: Dynamic NAT (pool-based)

### СТЪПКА 5: Конфигуриране на Dynamic NAT

**Сценарий:** Имаме pool от 10 публични IP адреса (203.0.113.10-19) за динамична употреба

```cisco
configure terminal

! Създай ACL за inside addresses (кои private IPs могат да използват NAT)
access-list 1 remark Inside hosts allowed to use NAT
access-list 1 permit 192.168.10.0 0.0.0.255

! Създай NAT pool (диапазон от публични IPs)
ip nat pool PUBLIC_POOL 203.0.113.10 203.0.113.19 netmask 255.255.255.0

! Свържи ACL с NAT pool
ip nat inside source list 1 pool PUBLIC_POOL

end
copy running-config startup-config
```

**Обяснение:**
- `access-list 1` - определя кои вътрешни IP адреси могат да използват NAT
- `ip nat pool PUBLIC_POOL` - създава pool от 10 публични IP адреса (203.0.113.10 до .19)
- `ip nat inside source list 1 pool PUBLIC_POOL` - свързва ACL 1 с pool-а

### СТЪПКА 6: Тестване на Dynamic NAT

**Тест 1: Генерирай трафик от PC1**
От PC1:
```
C:\> ping 8.8.8.8
```

**Тест 2: Виж динамичните mappings**
На R1-NAT:
```cisco
show ip nat translations

! Очакван изход:
Pro Inside global      Inside local       Outside local      Outside global
icmp 203.0.113.10:1    192.168.10.10:1    8.8.8.8:1          8.8.8.8:1
     ^^^^^^^^^^^^
     Взет от pool-а!
```

**Тест 3: Статистики**
```cisco
show ip nat statistics

! Total active translations: 1 (0 static, 1 dynamic; 1 extended)
! Peak translations: 1, occurred 00:00:12 ago
! Outside interfaces: Serial0/0/0
! Inside interfaces: GigabitEthernet0/0
! Hits: 5  Misses: 0
! Dynamic mappings:
! -- Inside Source
! [Id: 1] access-list 1 pool PUBLIC_POOL refcount 1
!  pool PUBLIC_POOL: netmask 255.255.255.0
!     start 203.0.113.10 end 203.0.113.19
!     type generic, total addresses 10, allocated 1 (10%), misses 0
```

**Проблем с Dynamic NAT:**
- Ако имаш 11+ едновременни потребители, но само 10 IP-та в pool-а → последните няма да имат достъп!
- **Решение:** Използвай PAT (следващата секция)

---

## ЧАСТ 3: PAT (NAT Overload) - Най-често използван

### СТЪПКА 7: Конфигуриране на PAT

**Сценарий:** Споделяме ЕДИН публичен IP адрес за всички вътрешни устройства (хиляди потребители с 1 IP!)

```cisco
configure terminal

! Премахни предишните NAT конфигурации
no ip nat inside source list 1 pool PUBLIC_POOL
no ip nat pool PUBLIC_POOL
no ip nat inside source static 192.168.10.50 203.0.113.50

! ACL за вътрешни адреси (позволи целия subnet)
access-list 1 remark PAT for all inside hosts
access-list 1 permit 192.168.10.0 0.0.0.255

! PAT using outside interface IP (overload)
ip nat inside source list 1 interface serial 0/0/0 overload

end
copy running-config startup-config
```

**Обяснение:**
- `overload` - **ключова дума за PAT** (Port Address Translation)
- `interface serial 0/0/0` - използва IP адреса на WAN интерфейса (209.165.200.2) за NAT
- Всички устройства споделят ЕДИН публичен IP, но се разграничават чрез **различни портове**

---

### СТЪПКА 8: Тестване на PAT

#### 8.1 Генерирай трафик от множество устройства

**От PC1:**
```
C:\> ping 8.8.8.8 -t
```

**От Server1:**
```
C:\> ping 1.1.1.1
```

#### 8.2 Виж PAT translations с портове

```cisco
show ip nat translations

! Очакван изход:
Pro Inside global           Inside local          Outside local         Outside global
icmp 209.165.200.2:1        192.168.10.10:1       8.8.8.8:1             8.8.8.8:1
icmp 209.165.200.2:2        192.168.10.50:1       1.1.1.1:2             1.1.1.1:2
     ^^^^^^^^^^^^^^         ^^^^^^^^^^^^^^^^
     СЪЩИЯТ публичен IP!    РАЗЛИЧНИ private IPs!
     Но РАЗЛИЧНИ портове!
```

**Забележи:**
- ✅ PC1 (192.168.10.10) и Server1 (192.168.10.50) споделят **същия публичен IP** (209.165.200.2)
- ✅ Те се разграничават чрез **различни port numbers** (:1, :2)
- ✅ До 65,535 едновременни връзки на един IP адрес!

#### 8.3 Статистики

```cisco
show ip nat statistics

! Total active translations: 2 (0 static, 0 dynamic; 2 extended)
! Outside interfaces: Serial0/0/0
! Inside interfaces: GigabitEthernet0/0
! Hits: 20  Misses: 0
! Dynamic mappings:
! -- Inside Source
! [Id: 1] access-list 1 interface Serial0/0/0 refcount 2
```

---

## ЧАСТ 4: PAT с конкретен IP (алтернативен метод)

### СТЪПКА 9: PAT с pool от един IP

Вместо да използваш интерфейса директно, можеш да създадеш pool с ЕДИН IP:

```cisco
configure terminal

! Премахни предишната PAT конфигурация
no ip nat inside source list 1 interface serial 0/0/0 overload

! Създай pool с ЕДИН IP
ip nat pool PAT_POOL 203.0.113.1 203.0.113.1 netmask 255.255.255.0

! PAT с този pool
ip nat inside source list 1 pool PAT_POOL overload

end
copy running-config startup-config
```

**Разлика:**
- **Метод 1** (`interface S0/0/0 overload`) → Използва IP-то на WAN интерфейса (209.165.200.2)
- **Метод 2** (`pool PAT_POOL overload`) → Използва специфичен IP (203.0.113.1)

**Кога да използваш Метод 2?**
- Когато имаш специален публичен IP за NAT (различен от WAN IP-то)
- Когато искаш по-добър контрол над използваните адреси

Тествай отново:
```cisco
show ip nat translations
```

Сега ще видиш 203.0.113.1 вместо 209.165.200.2 в Inside Global колоната.

---

## ЧАСТ 5: Комбиниране на Static NAT + PAT

### СТЪПКА 10: Real-World сценарий

**Сценарий:** Компания с Web server + 50 служители

**Изисквания:**
1. Web Server (192.168.10.50) трябва да е достъпен от интернет на 203.0.113.50
2. Всички служители трябва да имат интернет достъп чрез PAT

**Конфигурация:**

```cisco
configure terminal

! 1. Static NAT за Web Server
ip nat inside source static 192.168.10.50 203.0.113.50

! 2. ACL за служителите (exclude Web Server)
access-list 1 remark PAT for employees
access-list 1 deny   192.168.10.50
access-list 1 permit 192.168.10.0 0.0.0.255

! 3. PAT за служителите
ip nat inside source list 1 interface serial 0/0/0 overload

! 4. Inside/Outside интерфейси (ако липсват)
interface gigabitEthernet 0/0
 ip nat inside
interface serial 0/0/0
 ip nat outside

end
copy running-config startup-config
```

**Обяснение:**
- Static NAT има приоритет над Dynamic/PAT
- ACL 1 изрично **deny** Web Server IP-то (192.168.10.50)
- Всички останали адреси (192.168.10.0/24) използват PAT

**Тестване:**

```cisco
show ip nat translations

! Очакван изход:
Pro Inside global           Inside local          Outside local         Outside global
--- 203.0.113.50            192.168.10.50         ---                   ---
tcp 209.165.200.2:1024      192.168.10.10:1024    8.8.8.8:80            8.8.8.8:80
tcp 209.165.200.2:1025      192.168.10.11:1025    8.8.8.8:80            8.8.8.8:80
    ^^^^^^^^^^^^^^^^^
    Static NAT за server
    PAT за employees
```

---

## ЧАСТ 6: Port Forwarding (Static PAT)

### СТЪПКА 11: Конфигуриране на Port Forwarding

**Сценарий:** Web server слуша на нестандартен порт 8080 вътрешно, но искаш да е достъпен на порт 80 външно.

```cisco
configure terminal

! Port Forwarding - external port 80 → internal port 8080
ip nat inside source static tcp 192.168.10.50 8080 203.0.113.50 80 extendable

end
copy running-config startup-config
```

**Обяснение:**
- `tcp` - протокол
- `192.168.10.50 8080` - вътрешен IP:порт
- `203.0.113.50 80` - външен IP:порт
- `extendable` - позволява множество mappings

**Примери за реални случаи:**

```cisco
! SSH (external :22 → internal :22)
ip nat inside source static tcp 192.168.10.50 22 203.0.113.50 22

! RDP (external :3389 → internal :3389)
ip nat inside source static tcp 192.168.10.51 3389 203.0.113.50 3389

! FTP (external :21 → internal :21)
ip nat inside source static tcp 192.168.10.52 21 203.0.113.50 21
```

---

## VERIFICATION COMMANDS

### Основни команди за проверка:

```cisco
! 1. Виж всички NAT translations
show ip nat translations

! 2. Виж детайлна информация (timeouts, creation time)
show ip nat translations verbose

! 3. Статистики (hits, misses, pool utilization)
show ip nat statistics

! 4. Виж ACL конфигурацията
show access-lists

! 5. Виж NAT конфигурацията в running-config
show running-config | include nat

! 6. Дебъг (внимавай - много output!)
debug ip nat

! 7. Дебъг само за specific IP
debug ip nat 192.168.10.10

! 8. Изключи debug
undebug all
```

### Изчистване на NAT translations:

```cisco
! Изчисти всички динамични NAT entries
clear ip nat translation *

! Изчисти конкретен inside IP
clear ip nat translation inside 192.168.10.10

! Изчисти конкретен outside IP
clear ip nat translation outside 8.8.8.8

! Изчисти статистиките
clear ip nat statistics
```

---

## VERIFICATION CHECKLIST

### ✅ Static NAT:
```
☐ Сървърът има private IP (192.168.10.50)
☐ Static mapping е конфигуриран (203.0.113.50)
☐ ip nat inside/outside са зададени на интерфейсите
☐ Ping от ISP към public IP (203.0.113.50) работи
☐ show ip nat translations показва static mapping
☐ show ip nat statistics показва hits > 0
```

### ✅ Dynamic NAT:
```
☐ ACL permit вътрешни адреси
☐ NAT pool е създаден с достатъчно IPs
☐ ACL е свързан с pool (ip nat inside source list X pool Y)
☐ Ping от inside към outside работи
☐ show ip nat translations показва динамични mappings
☐ Pool utilization < 100% (има свободни IPs)
```

### ✅ PAT (Overload):
```
☐ ACL permit вътрешни адреси
☐ "overload" keyword е използван
☐ ip nat inside/outside са зададени
☐ Множество устройства могат да ping интернет едновременно
☐ show ip nat translations показва различни портове за всяко устройство
☐ Ping от PC1 към 8.8.8.8 работи
☐ Ping от Server1 към 1.1.1.1 работи едновременно
```

---

## ЧЕСТО СРЕЩАНИ ПРОБЛЕМИ

### Проблем 1: NAT не работи

**Симптоми:**
- Ping от inside към outside не работи
- `show ip nat translations` е празно

**Причина 1:** Inside/Outside интерфейси не са конфигурирани

**Решение:**
```cisco
show ip interface brief

! Виж дали има "(NAT inside)" или "(NAT outside)" до интерфейсите
! Ако липсва:
interface gigabitEthernet 0/0
 ip nat inside
 exit

interface serial 0/0/0
 ip nat outside
 exit
```

**Причина 2:** Default route липсва

**Решение:**
```cisco
show ip route

! Ако няма default route (0.0.0.0/0):
ip route 0.0.0.0 0.0.0.0 209.165.200.1
```

---

### Проблем 2: Няма translations

**Симптоми:**
- `show ip nat translations` е празно след генериране на трафик
- `show ip nat statistics` показва Misses > 0

**Причина:** ACL не permit-ва трафика

**Решение:**
```cisco
show access-lists

! Виж дали ACL е правилен
! Правилно:
access-list 1 permit 192.168.10.0 0.0.0.255

! Грешно:
access-list 1 deny 192.168.10.0 0.0.0.255
```

**Дебъг тест:**
```cisco
debug ip nat
! Генерирай трафик от PC
! Виж дали има "no valid source" съобщения
undebug all
```

---

### Проблем 3: Dynamic NAT pool е пълен

**Симптоми:**
- Само първите N устройства имат интернет (където N = pool size)
- `show ip nat statistics` показва "allocated 10 (100%)"

**Причина:** Повече устройства от IP адреси в pool-а

**Решение 1:** Използвай PAT (overload) вместо Dynamic NAT
```cisco
no ip nat inside source list 1 pool PUBLIC_POOL
ip nat inside source list 1 pool PUBLIC_POOL overload
```

**Решение 2:** Увеличи pool-а
```cisco
no ip nat pool PUBLIC_POOL
ip nat pool PUBLIC_POOL 203.0.113.10 203.0.113.50 netmask 255.255.255.0
! Сега имаш 41 IPs вместо 10
```

---

### Проблем 4: Ping работи, но HTTP не

**Симптоми:**
- `ping 8.8.8.8` работи
- Браузърът не може да зареди уебсайтове

**Причина 1:** DNS не работи

**Решение:**
```cisco
! Провери DNS на PC
C:\> ipconfig /all
! DNS Server трябва да е 8.8.8.8

! Тествай DNS
C:\> nslookup google.com
```

**Причина 2:** ACL блокира HTTP (port 80)

**Решение:**
```cisco
show ip access-lists

! Ако има ACL на интерфейсите, провери дали permit-ва TCP port 80
interface gigabitEthernet 0/0
 show ip access-group
 
! Премахни ACL временно за тест
no ip access-group 100 in
```

---

### Проблем 5: Serial интерфейсът е down

**Симптоми:**
- `show ip interface brief` показва Serial0/0/0: down/down

**Причина:** Липсва clock rate на DCE страната

**Решение:**

**Стъпка 1:** Определи коя е DCE страната
```
В Packet Tracer, кликни на кабела - виж малък часовник символ
```

**Стъпка 2:** Добави clock rate на DCE router
```cisco
! На ISP-Router (DCE страна):
interface serial 0/0/0
 clock rate 64000
 no shutdown
```

**Стъпка 3:** Активирай интерфейса на DTE router
```cisco
! На R1-NAT (DTE страна):
interface serial 0/0/0
 no shutdown
```

---

## TROUBLESHOOTING WORKFLOW

```
┌─────────────────────────────────────────────┐
│ СТЪПКА 1: Провери Layer 1/2                 │
│ • show ip interface brief                   │
│ • Всички интерфейси up/up?                  │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 2: Провери ip nat inside/outside     │
│ • show ip interface Gi0/0                   │
│ • show ip interface Se0/0/0                 │
│ • Виждаш ли "(NAT inside/outside)"?         │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 3: Провери ACL                       │
│ • show access-lists                         │
│ • ACL permit-ва ли inside subnet?           │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 4: Провери NAT config                │
│ • show running-config | include nat         │
│ • Има ли "ip nat inside source..."?         │
│ • Има ли "overload" за PAT?                 │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 5: Провери routing                   │
│ • show ip route                             │
│ • Има ли default route (0.0.0.0/0)?         │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 6: Генерирай трафик и дебъг          │
│ • debug ip nat                              │
│ • ping 8.8.8.8 (от PC)                      │
│ • Виждаш ли NAT translations?               │
└──────────────┬──────────────────────────────┘
               │ ✅ ДА
               ▼
┌─────────────────────────────────────────────┐
│ СТЪПКА 7: Провери translations              │
│ • show ip nat translations                  │
│ • Виждаш ли inside local → inside global?   │
│ • show ip nat statistics (Hits > 0?)        │
└─────────────────────────────────────────────┘
```

---

## ЗАДАЧИ ЗА САМОСТОЯТЕЛНА РАБОТА

### Задача 1: Port Forwarding за multiple services

Конфигурирай Port Forwarding за:
- Web Server (HTTP): 192.168.10.50:80 → 203.0.113.1:80
- SSH Server: 192.168.10.50:22 → 203.0.113.1:22
- FTP Server: 192.168.10.51:21 → 203.0.113.1:21

```cisco
ip nat inside source static tcp 192.168.10.50 80 203.0.113.1 80 extendable
ip nat inside source static tcp 192.168.10.50 22 203.0.113.1 22 extendable
ip nat inside source static tcp 192.168.10.51 21 203.0.113.1 21 extendable
```

---

### Задача 2: Multiple Static NAT mappings

Създай Static NAT за:
- Web Server: 192.168.10.50 → 203.0.113.50
- Mail Server: 192.168.10.51 → 203.0.113.51
- FTP Server: 192.168.10.52 → 203.0.113.52
- DNS Server: 192.168.10.53 → 203.0.113.53

**Стъпки:**
1. Конфигурирай Static NAT mappings
2. Добави PAT за останалите устройства
3. Тествай ping от ISP към всеки public IP
4. Провери `show ip nat translations`

---

### Задача 3: PAT с backup pool

**Сценарий:** Искаш да използваш interface IP за PAT, но ако той е зает, да fallback-ваш към pool.

```cisco
! Primary PAT
ip nat inside source list 1 interface serial 0/0/0 overload

! Backup pool (ако interface е зает)
ip nat pool BACKUP_POOL 203.0.113.100 203.0.113.110 netmask 255.255.255.0
ip nat inside source list 1 pool BACKUP_POOL overload
```

**Тест:** Генерирай много едновременни връзки (>1000) и виж кога се активира backup pool-ът.

---

### Задача 4: NAT с различни subnets

**Топология:**
```
        R1-NAT
       /      \
     SW1      SW2
    /  \      /  \
  PC1  PC2  PC3  PC4
 .10.x      .20.x
```

**Изисквания:**
- Subnet 192.168.10.0/24 → PAT с 203.0.113.1
- Subnet 192.168.20.0/24 → PAT с 203.0.113.2

**Конфигурация:**
```cisco
! ACL за subnet 1
access-list 10 permit 192.168.10.0 0.0.0.255

! ACL за subnet 2
access-list 20 permit 192.168.20.0 0.0.0.255

! PAT за всеки subnet с различен IP
ip nat pool POOL1 203.0.113.1 203.0.113.1 netmask 255.255.255.0
ip nat pool POOL2 203.0.113.2 203.0.113.2 netmask 255.255.255.0

ip nat inside source list 10 pool POOL1 overload
ip nat inside source list 20 pool POOL2 overload
```

---

## РЕАЛНИ СЦЕНАРИИ

### Сценарий 1: Малка фирма с 50 служители

**Инфраструктура:**
- 1 публичен IP от ISP
- 50 компютъра в офиса (192.168.1.0/24)
- WiFi за гости (192.168.2.0/24)

**Решение:**
```cisco
! PAT за всички устройства
access-list 1 permit 192.168.1.0 0.0.0.255
access-list 1 permit 192.168.2.0 0.0.0.255
ip nat inside source list 1 interface GigabitEthernet0/1 overload
```

**Предимства:**
- ✅ Един публичен IP за всички
- ✅ До 65,535 едновременни връзки
- ✅ Ниска цена

---

### Сценарий 2: Компания със собствен Web/Mail сървър

**Инфраструктура:**
- 3 публични IP адреса (203.0.113.10-12)
- Web Server: 192.168.1.10
- Mail Server: 192.168.1.11
- 100 служители

**Решение:**
```cisco
! Static NAT за сървъри
ip nat inside source static 192.168.1.10 203.0.113.10
ip nat inside source static 192.168.1.11 203.0.113.11

! PAT за служители (exclude сървърите)
access-list 1 deny   192.168.1.10
access-list 1 deny   192.168.1.11
access-list 1 permit 192.168.1.0 0.0.0.255
ip nat inside source list 1 interface GigabitEthernet0/1 overload
```

**Предимства:**
- ✅ Сървърите са винаги на същия public IP
- ✅ Служителите споделят оставащия IP
- ✅ Security (скрити служителски IPs)

---

### Сценарий 3: Data Center с множество публични IP-та

**Инфраструктура:**
- 100 публични IP адреса (203.0.113.10-109)
- 500 виртуални машини
- Load balancer

**Решение:**
```cisco
! Dynamic NAT pool
ip nat pool DATACENTER_POOL 203.0.113.10 203.0.113.109 netmask 255.255.255.0
access-list 1 permit 192.168.0.0 0.0.255.255
ip nat inside source list 1 pool DATACENTER_POOL overload
```

**Предимства:**
- ✅ Flexibility (500 VMs, 100 IPs)
- ✅ PAT overload за допълнителен капацитет
- ✅ Automatic load distribution

---

### Сценарий 4: ISP с CGNAT (Carrier-Grade NAT)

**Инфраструктура:**
- 10,000 home users
- 1,000 публични IP адреса

**Решение:**
```cisco
! Огромен PAT pool
ip nat pool ISP_POOL 100.64.0.1 100.67.255.254 netmask 255.252.0.0
access-list 1 permit 10.0.0.0 0.255.255.255
ip nat inside source list 1 pool ISP_POOL overload
```

**Особености:**
- RFC 6598 адреси (100.64.0.0/10)
- "Shared Address Space" за CGNAT
- Проблеми с gaming, P2P

---

## КЛЮЧОВИ КОНЦЕПЦИИ

### NAT Терминология:

```
┌─────────────────────────────────────────────────────────┐
│                    NAT ROUTER                           │
│                                                         │
│  Inside Local ──────NAT──────► Inside Global           │
│  (Private IP)                  (Public IP)             │
│  192.168.10.10 ──────────────► 203.0.113.10            │
│                                                         │
│                        ▲                                │
│                        │                                │
│                        ▼                                │
│                                                         │
│  Outside Global ◄────────────── Outside Local          │
│  (Actual dest IP)              (Dest as seen inside)   │
│  8.8.8.8 ◄───────────────────── 8.8.8.8                │
└─────────────────────────────────────────────────────────┘
```

**Обяснение:**
- **Inside Local:** IP адресът, който устройството има в локалната мрежа (private)
- **Inside Global:** IP адресът, който устройството получава при излизане навън (public)
- **Outside Local:** IP адресът на външното устройство, както се вижда от вътре
- **Outside Global:** Действителният IP адрес на външното устройство

**Пример:**
- PC1 (192.168.10.10) прави ping към Google DNS (8.8.8.8)
- Inside Local: 192.168.10.10
- Inside Global: 203.0.113.10 (след NAT)
- Outside Local: 8.8.8.8 (видян от PC1)
- Outside Global: 8.8.8.8 (действителен)

---

### Best Practices:

1. ✅ **Използвай PAT за повечето случаи**
   - Икономия на публични IP адреси
   - Поддържа хиляди едновременни връзки
   - По-лесна конфигурация

2. ✅ **Static NAT само за сървъри**
   - Web, Mail, FTP, DNS сървъри
   - Когато трябва входяща връзка от интернет
   - Документирай всеки mapping

3. ✅ **ACL трябва да е конкретен**
   ```cisco
   ! ДОБРО
   access-list 1 permit 192.168.10.0 0.0.0.255
   
   ! ЛОШО
   access-list 1 permit any
   ```

4. ✅ **Документирай NAT конфигурацията**
   ```cisco
   access-list 1 remark NAT for employee subnet
   access-list 1 permit 192.168.10.0 0.0.0.255
   
   ! Web Server Static NAT
   ip nat inside source static 192.168.10.50 203.0.113.50
   ```

5. ✅ **Мониторинг и статистики**
   ```cisco
   ! Ежедневна проверка
   show ip nat statistics
   show ip nat translations | count
   
   ! Проверка на pool utilization
   show ip nat statistics | include allocated
   ```

6. ✅ **Security considerations**
   - NAT не е firewall!
   - Използвай ACL за допълнителна сигурност
   - Ограничи достъпа до Static NAT сървъри

---

### Често срещани заблуди:

❌ **Заблуда 1:** "NAT е firewall"
✅ **Реалност:** NAT скрива IP адреси, но не филтрира трафик. Нужен е ACL/Firewall.

❌ **Заблуда 2:** "Dynamic NAT е по-добър от PAT"
✅ **Реалност:** PAT е по-ефективен - споделя 1 IP за хиляди устройства.

❌ **Заблуда 3:** "Static NAT е постоянен, не може да се промени"
✅ **Реалност:** Static NAT може да се премахне/промени по всяко време.

❌ **Заблуда 4:** "NAT решава IPv4 exhaustion проблема"
✅ **Реалност:** NAT е временно решение. IPv6 е дългосрочното решение.

---

## ВРЪЗКА С LAB 9 (INTEGRATED NETWORK)

Този лаб е **подготовка за Lab 9**, където ще изградиш пълна enterprise мрежа:

### Какво ще използваш от Lab 8 в Lab 9:

1. **ISP Router топология**
   - Lab 9 използва същата ISP Router структура
   - Loopback интерфейси за симулация на интернет

2. **PAT конфигурация**
   - Lab 9 има 3+ subnets, всички използват PAT
   - Ще трябва да адаптираш ACL за multiple subnets

3. **Static NAT за сървъри**
   - Lab 9 има DNS и Web сървъри с Static NAT
   - Port Forwarding за специфични услуги

4. **Troubleshooting skills**
   - Lab 9 е по-сложен - ще трябва да troubleshoot-ваш NAT проблеми
   - Verification commands са същите

### Пример NAT в Lab 9:

```cisco
! Lab 9 има 3 subnets: VLAN 10, 20, 50
access-list 1 permit 172.16.10.0 0.0.0.255    ! VLAN 10
access-list 1 permit 172.16.20.0 0.0.0.255    ! VLAN 20
access-list 1 permit 172.16.50.0 0.0.0.255    ! VLAN 50

! PAT за всички
ip nat inside source list 1 interface Serial0/0/0 overload

! Static NAT за DNS Server
ip nat inside source static 172.16.50.100 203.0.113.100
```

**Ключови разлики в Lab 9:**
- Множество VLANs вместо един subnet
- Inter-VLAN routing + NAT
- ACL интеграция с NAT
- По-сложна топология (Core/Distribution/Access)

---

## СЛЕДВАЩИ СТЪПКИ

Сега знаеш как да:
- ✅ Конфигурираш Static NAT за сървъри
- ✅ Конфигурираш Dynamic NAT с pools
- ✅ Конфигурираш PAT (NAT Overload) за споделяне на IP
- ✅ Симулираш реалистична ISP връзка с Loopback интерфейси
- ✅ Troubleshoot NAT проблеми
- ✅ Комбинираш Static NAT + PAT в production сценарии

**Следващ лаб:** Lab 9 - Integrated Network Design (VLANs + DHCP + DNS + NAT + ACL)

---

## ДОПЪЛНИТЕЛНИ РЕСУРСИ

### Cisco Documentation:
- [Cisco NAT Configuration Guide](https://www.cisco.com/c/en/us/td/docs/ios-xml/ios/ipaddr_nat/configuration/xe-16/nat-xe-16-book.html)
- RFC 1918 - Private Address Space
- RFC 6598 - Shared Address Space (CGNAT)

### Packet Tracer Tips:
- Използвай "Simulation Mode" за да видиш NAT в действие
- Добави PDU (Simple PDU) между устройствата
- Виж как се променя source IP при преминаване през NAT router

### За практика:
- Експериментирай с различни NAT типове
- Създай сложни топологии с множество NAT routers
- Симулирай real-world сценарии (DMZ, multi-tier applications)

---

**Готово! Запази .pkt файла като `Lab8_NAT_Configuration_REVISED.pkt`**

**ВНИМАНИЕ:** Този лаб използва **ISP Router** вместо Cloud устройство за реалистична симулация. В реален production environment, ISP Router-ът би бил управляван от вашия Internet Service Provider.
