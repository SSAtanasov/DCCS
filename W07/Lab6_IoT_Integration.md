# УПРАЖНЕНИЕ 6: IoT ИНТЕГРАЦИЯ И АВТОМАТИЗАЦИЯ
## Smart Office с IoT устройства в Packet Tracer

**Цел:** Да се научите да интегрирате IoT устройства, да ги програмирате и да създавате автоматизация.

**Продължителност:** 90-120 минути

**Prerequisite:** Завършени Упражнения 1-5

---

## ЧАСТ 1: IoT архитектура в Packet Tracer (15 мин)

### Какво е IoT (Internet of Things)?

Мрежа от физически устройства с вградени сензори, софтуер и мрежова свързаност, които събират и обменят данни.

### IoT архитектура:

```
IoT Устройства → WiFi → IoT Server → SBC-PT/MCU-PT → Автоматизация
```

### Основни компоненти:

**Мрежова инфраструктура:**
- **Access Point** - безжична точка за достъп
- **IoT Server** - централен сървър за регистрация и управление

**Контролери за автоматизация:**
- **SBC-PT** (Single Board Computer) - контролер с Blockly и Python
- **MCU-PT** (Microcontroller Unit) - микроконтролер само с Blockly

**IoT устройства:**
- **Sensors** - сензори (движение, температура, светлина, дим)
- **Actuators** - актуатори (лампи, вентилатори, врати, сирени)

### SBC-PT vs MCU-PT - Коя да изберем?

| Характеристика | MCU-PT | SBC-PT |
|----------------|--------|--------|
| Реален аналог | Arduino, ESP32 | Raspberry Pi |
| Програмиране | Само Blockly | Blockly + Python |
| Сложност | Прости if-then логики | Сложна логика, променливи |
| Брой устройства | 2-3 сензора | Множество устройства |
| Употреба | Основни автоматизации | Професионални проекти |

**Препоръка за упражнението:** Използваме **SBC-PT**, защото:
- По-универсален и мощен
- Подходящ за курсови проекти
- Python като бонус възможност

**Забележка:** MCU-PT може да се използва алтернативно за прости задачи. Конфигурацията е идентична - само програмирането е по-ограничено.

---

## ЧАСТ 2: Топология на мрежата (15 мин)

### Разширена топология:

```
                [Router R1]
                     |
              GigE0/0 (trunk)
                     |
                [Switch SW1]
          /     |      |       \        \
       DNS   Admin   IoT    Access   SBC-PT
      Server  PC3   Server   Point
                                |
                           (Wireless)
                           /    |    \
                      Motion  Smart  Temp
                      Detect   LED   Sensor
```

### Нови устройства:

**Мрежови компоненти:**
1. Access Point-PT - за WiFi
2. Server-PT (IoT Server) - за регистрация
3. SBC-PT - за програмиране

**IoT устройства:**
1. Motion Detector (Sensors)
2. Smart LED (Actuators)
3. Temperature Monitor (Sensors)
4. Fan (Actuators)

---

## ЧАСТ 3: Конфигурация на Access Point (10 мин)

### Стъпка 1: Добавяне и свързване

- Network Devices → Wireless Devices → Access Point-PT
- FastEthernet0 на AP → SW1 FastEthernet0/8

### Стъпка 2: Конфигурация на порта

```cisco
SW1(config)# interface FastEthernet0/8
SW1(config-if)# switchport mode access
SW1(config-if)# switchport access vlan 20
SW1(config-if)# description IoT-AccessPoint
```

### Стъпка 3: IP конфигурация

**Access Point → Config → Port 1:**
```
IP Address: 192.168.20.51
Subnet Mask: 255.255.255.0
Default Gateway: 192.168.20.1
```

### Стъпка 4: WiFi конфигурация

**Access Point → Config → Wireless (порт 0):**
```
SSID: SmartOffice
Authentication: WPA2-PSK
PSK Pass Phrase: SecureIoT123
```

### Проверка:
```bash
ping 192.168.20.51    # От Admin PC3
```

---

## ЧАСТ 4: Конфигурация на IoT Server (10 мин)

### Стъпка 1: Добавяне

- End Devices → Server-PT
- Свържете към SW1 FastEthernet0/9

### Стъпка 2: Конфигурация

```cisco
SW1(config)# interface FastEthernet0/9
SW1(config-if)# switchport mode access
SW1(config-if)# switchport access vlan 20
SW1(config-if)# description IoT-Server
```

### Стъпка 3: IP адрес

**Server → Config → FastEthernet0:**
```
IP Address: 192.168.20.100
Subnet Mask: 255.255.255.0
Default Gateway: 192.168.20.1
```

### Стъпка 4: Активиране на IoT услуга

**Server → Services → IoT:**
```
Service: ON
Port: 8080
```

### Проверка - достъп до Dashboard:
```
http://192.168.20.100:8080    # От Admin PC3 → Web Browser
```

---

## ЧАСТ 5: Добавяне на IoT устройства (20 мин)

### Motion Detector (Sensor)

**1. Добавяне:**
- IoT → Sensors → Motion Detector

**2. WiFi конфигурация:**
- Config → Wireless0
- SSID: SmartOffice
- Authentication: WPA2-PSK
- PSK Pass Phrase: SecureIoT123

**3. Регистрация към IoT Server:**
- Config → IoT Server
- Server IP: 192.168.20.100
- Registration Port: 8080
- User: admin
- Password: admin
- **Connect**

### Smart LED (Actuator)

**Повторете същите стъпки:**
1. IoT → Actuators → Smart LED
2. WiFi конфигурация (SmartOffice)
3. Регистрация (192.168.20.100:8080)

### Тест на ръчен контрол:

**Admin PC3 → Web Browser → http://192.168.20.100:8080**

- Виждате устройствата в списъка
- Кликнете на Smart LED
- Toggle ON/OFF → лампата трябва да светне/угасне

**Забележка:** Запишете точните имена от Dashboard (напр. "MotionDetector0", "SmartLED0") - ще ви трябват за Blockly!

---

## ЧАСТ 6: Добавяне на SBC-PT контролер (10 мин)

### Стъпка 1: Добавяне

- End Devices → SBC-PT
- FastEthernet → SW1 FastEthernet0/10

### Стъпка 2: Конфигурация

```cisco
SW1(config)# interface FastEthernet0/10
SW1(config-if)# switchport mode access
SW1(config-if)# switchport access vlan 20
SW1(config-if)# description IoT-Controller-SBC
```

### Стъпка 3: IP адрес

**SBC-PT → Config → FastEthernet0:**
```
IP Address: 192.168.20.60
Subnet Mask: 255.255.255.0
Default Gateway: 192.168.20.1
```

### Стъпка 4: Връзка с IoT Server

**SBC-PT → Config → Settings:**
```
IoT Server IP: 192.168.20.100
Port: 8080
```

---

## ЧАСТ 7: Програмиране с Blockly (30 мин)

### Сценарий: Motion Detector → Smart LED

### Стъпка 1: Отваряне на Programming

**SBC-PT → Programming → Blockly**

### Стъпка 2: Проверка на имената

**ВАЖНО!** От Dashboard вижте точните имена:
- Motion Detector: "MotionDetector0"
- Smart LED: "SmartLED0"

### Стъпка 3: Blockly програма - включване

```
┌─────────────────────────────────────────┐
│ Events:                                  │
│   When [MotionDetector0] detects motion │
│ Actions:                                 │
│   Set device [SmartLED0] state to ON    │
└─────────────────────────────────────────┘
```

**Блокове:**
1. Events → "When device detects..." → MotionDetector0 → detects motion
2. Actions → "Set device status..." → SmartLED0 → ON

### Стъпка 4: Blockly програма - изключване

```
┌─────────────────────────────────────────┐
│ Events:                                  │
│   When [MotionDetector0] no motion      │
│ Actions:                                 │
│   Set device [SmartLED0] state to OFF   │
└─────────────────────────────────────────┘
```

### Стъпка 5: Стартиране

**Кликнете: Start** (или Run)

### Стъпка 6: Тестване

**Simulation Mode:**
1. Долу дясно → Simulation Mode
2. Кликнете Motion Detector
3. Активирайте движение (червена зона)
4. **LED трябва да светне автоматично!**

---

## ЧАСТ 8: Разширена автоматизация - Temperature (BONUS)

### Добавете устройства:
- Temperature Monitor (Sensor)
- Fan (Actuator)
- Регистрирайте ги към IoT Server

### Blockly логика:

```
┌─────────────────────────────────────────┐
│ Control:                                 │
│   If [TemperatureSensor0] value > 26    │
│   then:                                  │
│       Set [Fan0] to ON                   │
│   else:                                  │
│       Set [Fan0] to OFF                  │
└─────────────────────────────────────────┘
```

**Блокове:**
1. Control → "if... then... else..."
2. Sensors → "Get sensor value" → TemperatureSensor0
3. Operators → ">" → Number: 26
4. Actions → "Set device status" → Fan0 → ON/OFF

---

## ЧАСТ 9: IoT Security (15 мин)

### 1. Отделен VLAN за IoT

```cisco
R1(config)# vlan 30
R1(config-vlan)# name IoT_Devices
R1(config-vlan)# exit
R1(config)# interface GigabitEthernet0/0.30
R1(config-subif)# encapsulation dot1Q 30
R1(config-subif)# ip address 192.168.30.1 255.255.255.0
```

### 2. ACL за ограничаване на трафика

```cisco
R1(config)# access-list 120 remark IoT-Security-Policy
R1(config)# access-list 120 permit ip 192.168.30.0 0.0.0.255 host 192.168.20.100
R1(config)# access-list 120 permit ip 192.168.30.0 0.0.0.255 host 192.168.20.60
R1(config)# access-list 120 deny ip 192.168.30.0 0.0.0.255 any log
R1(config)# access-list 120 permit ip any any
R1(config)# interface GigabitEthernet0/0.30
R1(config-subif)# ip access-group 120 in
```

**Обяснение:**
- IoT устройства могат да достъпват само IoT Server и SBC-PT
- Блокира достъп до други мрежи

### 3. Силни пароли

- IoT Server: сменете admin/admin
- WiFi: сменете SecureIoT123 с по-силна парола
- Access Point: сменете административната парола

---

## ЧАСТ 10: Примерни IoT сценарии

### Сценарий 1: Smart Security System
```
Motion + Door Sensor → Siren + Red LED
Smoke Detector → Siren + Notification
```

### Сценарий 2: Energy Management
```
Light Sensor + Motion → Smart LED ON
No Motion (5 min) → All devices OFF
Temperature → Fan/Heater control
```

### Сценарий 3: Environmental Monitoring
```
CO2 > 800ppm → Window OPEN + Fan ON
Humidity > 70% → Dehumidifier ON
```

---

## ЧАСТ 11: Troubleshooting

### Проблем 1: Устройство не се свързва към WiFi

**Проверки:**
- SSID правилен ли е? (case sensitive!)
- Парола правилна ли е?
- Access Point включен ли е?
- Разстояние до AP прекалено голямо ли е?

**Решение:**
- Изтрийте и въведете отново WiFi данните
- Приближете устройството до AP

### Проблем 2: Не се регистрира към IoT Server

**Проверки:**
- IoT Services активирана ли е? (ON)
- IP адрес правилен ли е? (192.168.20.100)
- Port правилен ли е? (8080)

**Решение:**
- ping 192.168.20.100 от PC3
- Проверете IoT Server → Services → IoT
- Въведете отново регистрационните данни

### Проблем 3: Blockly програмата не работи

**Проверки:**
- SBC свързан към IoT Server?
- Имената на устройствата точни ли са?
- Програмата стартирана ли е? (Start)

**Решение:**
- Проверете имената от Dashboard
- Променете ги в Blockly да съвпадат точно
- Stop → Start програмата

### Проблем 4: Ръчният контрол не работи

**Решение:**
- Refresh Dashboard страницата
- Проверете WiFi връзката на устройството
- Регистрирайте устройството отново

---

## ЧАСТ 12: Документация

### Таблица с устройства:

| Device Name       | Type       | IP Address    | Function              |
|-------------------|------------|---------------|-----------------------|
| IoT-Server        | Server-PT  | 192.168.20.100| Регистрация, контрол  |
| SBC-PT-1          | Controller | 192.168.20.60 | Автоматизация         |
| Access-Point-1    | AP         | 192.168.20.51 | WiFi свързаност       |
| Motion-Det-1      | Sensor     | (Wireless)    | Детекция на движение  |
| Smart-LED-1       | Actuator   | (Wireless)    | Осветление            |
| Temp-Monitor-1    | Sensor     | (Wireless)    | Температура           |
| Fan-1             | Actuator   | (Wireless)    | Вентилация            |

### Мрежови параметри:

| Параметър       | Стойност           |
|-----------------|-------------------|
| WiFi SSID       | SmartOffice       |
| WiFi Security   | WPA2-PSK          |
| WiFi Password   | SecureIoT123      |
| IoT Server IP   | 192.168.20.100    |
| IoT Server Port | 8080              |
| Dashboard URL   | http://192.168.20.100:8080 |

---

## ЧАСТ 13: Checklist за завършване

```
☐ Access Point конфигуриран с WiFi
☐ IoT Server активиран (port 8080)
☐ 2+ IoT сензора добавени и регистрирани
☐ 2+ IoT актуатора добавени и регистрирани
☐ SBC-PT добавен и свързан към IoT Server
☐ Blockly програма написана
☐ Автоматизацията работи в Simulation Mode
☐ Ръчното управление през Dashboard работи
☐ ACL за IoT security (опционално)
☐ Документация попълнена
```

---

## ЧАСТ 14: Какво научихме

1. ✅ IoT архитектура и компоненти
2. ✅ Разлика между SBC-PT и MCU-PT
3. ✅ Конфигурация на Access Point
4. ✅ IoT Server за централизиран контрол
5. ✅ Регистрация на IoT устройства
6. ✅ Ръчен контрол през Web Dashboard
7. ✅ Blockly визуално програмиране
8. ✅ Event-driven автоматизация
9. ✅ IoT сигурност (VLAN, ACL)
10. ✅ Troubleshooting на IoT мрежи

---

## ЧАСТ 15: Подготовка за курсовия проект

### Вие вече знаете (Упражнения 1-6):

- ✅ Мрежов дизайн
- ✅ VLAN и Inter-VLAN routing
- ✅ DHCP и DNS
- ✅ ACL за сигурност
- ✅ IoT интеграция и автоматизация

### Примерна структура на курсов проект:

```
1. Мрежова топология (Router + Switches)
2. VLAN сегментация (Users, IT, IoT)
3. DHCP за автоматично адресиране
4. DNS server
5. ACL за security (включително IoT)
6. IoT система (3-5 устройства)
7. Blockly автоматизация
8. Документация (5-8 стр PDF)
9. Презентация и demo
```

### Примерни теми:

- **Smart Office Building** - автоматизация на офис
- **University Campus** - учебни зали с IoT
- **Smart Factory** - индустриална автоматизация
- **Smart Home** - интелигентен дом
- **Hospital IoT** - болничен мониторинг

---

## Полезни команди

**Мрежова проверка:**
```bash
ping 192.168.20.100    # IoT Server
ping 192.168.20.51     # Access Point
```

**Dashboard:**
```
http://192.168.20.100:8080
```

**Cisco debugging:**
```cisco
show ip interface brief
show vlan brief
show access-lists
show running-config
```

---

**Запазете файла като:** `Lab6_IoT_YourName.pkt`

**Документация като:** `Lab6_Documentation_YourName.pdf`

---

**Успех с упражнението! 🎓**

**Автор:** гл. ас. Светослав Атанасов  
**Тракийски университет - Стара Загора**  
**Курс:** Проектиране на комуникационни и компютърни системи
